# Copiar para .env na raiz do registro-tour (compose de produção na VPS Subway)
# Backend Express desligado — Hub API (/web) via gateway nginx /backend/
# EV counter: ~/Subway-Ev-Counter/.env com DATABASE_URL

# --- Domínios públicos (DNS A → VPS vps-aba40034) ---
DOMAIN_FRONTEND=registro-ds.vako.pt
DOMAIN_API=api.registro-oldgen.vako.pt
DOMAIN_EV_COUNTER=ev-counter.vako.pt

# Pastas em /etc/letsencrypt/live/
SSL_CERT_DIR_FRONTEND=registro-ds.vako.pt
SSL_CERT_DIR_API=api.registro-oldgen.vako.pt
SSL_CERT_DIR_EV_COUNTER=ev-counter.vako.pt

# Same-origin no build do React (nginx injeta HUB_KEY_REGISTRO)
VITE_API_URL=/backend

# Hub (VPS distinta) — gateway /backend → ${HUB_WEB_UPSTREAM}/web/
HUB_WEB_UPSTREAM=https://apiv2.subway.vako.pt
HUB_WEB_HOST=apiv2.subway.vako.pt
HUB_KEY_REGISTRO=

# Upstream interno do nginx para o container frontend
FRONTEND_UPSTREAM=frontend:80

LETSENCRYPT_DIR=/etc/letsencrypt

# Build do ev_counter (pasta irmã na VPS: ~/Subway-Ev-Counter)
# Monorepo local: EV_COUNTER_BUILD_CONTEXT=../ev_counter
EV_COUNTER_BUILD_CONTEXT=../Subway-Ev-Counter
EV_COUNTER_ENV_FILE=../Subway-Ev-Counter/.env

HTTP_PORT=80
HTTPS_PORT=443
