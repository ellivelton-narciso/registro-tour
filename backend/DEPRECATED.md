# Express descontinuado (2026-07)

A API de torneios foi migrada para [[Subway-Hub-API-Rank]] sob `/web`.

Este diretório (`backend/`) permanece só como referência histórica até remoção do repositório.
Não subir o serviço `api` no `docker-compose.yml` da raiz.
